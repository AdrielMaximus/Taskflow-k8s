apiVersion: v1
kind: Service
metadata:
  name: {{ include "taskflow-k8s.fullname" . }}
  labels:
    {{- include "taskflow-k8s.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "taskflow-k8s.selectorLabels" . | nindent 4 }}
